# Alert: PCG Memory Engagement Threshold
# Detects when memory utilization reaches backpressure engagement level
name: PCG Memory Engagement Threshold

# Description and details
description: |
  PCG memory utilization has exceeded 75%, which is the backpressure engagement
  threshold. At this level:

  - Backpressure ENGAGES and starts rejecting requests
  - Upstream agents will receive 503 errors
  - Memory should stabilize or decrease as load is shed

  Memory thresholds:
  - 60%: Backpressure releases (hysteresis)
  - 75%: Backpressure engages (THIS ALERT)
  - 90%: Emergency brake
  - 100%: OOM Kill

  Container limit: 4 GiB
  GOMEMLIMIT: 3276 MiB (~3.2 GiB)

# Type of alert: BASELINE | STATIC
type: STATIC

# NRQL query
nrql:
  query: "SELECT latest(pcg_instance_memory_utilization) * 100 FROM Metric WHERE namespace = 'pcg-backpressure-processor' FACET pod_name"

# Function used to aggregate the NRQL query value(s) for comparison to the terms.threshold (Default: SINGLE_VALUE)
valueFunction: SINGLE_VALUE

# List of Critical and Warning thresholds for the condition
terms:
  - priority: WARNING
    # Operator used to compare against the threshold.
    operator: ABOVE
    # Value that triggers a violation (75% = backpressure engagement)
    threshold: 75
    # Time in seconds; 120 - 3600 (2 minutes)
    thresholdDuration: 120
    # How many data points must be in violation for the duration
    thresholdOccurrences: ALL

# Duration after which a violation automatically closes
# Time in seconds; 300 - 2592000 (Default: 86400 [1 day])
violationTimeLimitSeconds: 259200
