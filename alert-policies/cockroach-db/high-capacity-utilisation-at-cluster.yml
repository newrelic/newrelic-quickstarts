name: High capacity utilisation at cluster

description: |+
  This alert is triggered when the capacity utilisation is above 80% for at least 60 minutes.
type: STATIC
nrql:
  query: "SELECT ((average(`Max capacity`)) - (average(`Available capacity`) ))*100/ (average(`Max capacity`))  As 'Used % Capacity' FROM (SELECT (average(capacity) ) AS `Max capacity`, (average(capacity_available) ) AS `Available capacity` FROM Metric  WHERE ((job = 'cockroachdb') and instrumentation.name='remote-write')  FACET instance )"

# Function used to aggregate the NRQL query value(s) for comparison to the terms.threshold (Default: SINGLE_VALUE)
valueFunction: SINGLE_VALUE

# List of Critical and Warning thresholds for the condition
terms:
  - priority: CRITICAL
    # Operator used to compare against the threshold.
    operator: ABOVE
    # Value that triggers a violation
    threshold: 80
    # Time in seconds; 120 - 3600
    thresholdDuration: 3600
    # How many data points must be in violation for the duration
    thresholdOccurrences: ALL

# Duration after which a violation automatically closes
# Time in seconds; 300 - 2592000 (Default: 86400 [1 day])
violationTimeLimitSeconds: 86400