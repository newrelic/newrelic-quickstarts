on: workflow_dispatch

jobs:
  echo-input:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/github-script@v4
        run: npm install is-image
        with:
            script: |
              console.log('hello')
