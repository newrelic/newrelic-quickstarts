id: docker-otel
displayName: Docker (OpenTelemetry)
description: |
  The Open Telemetry Dockerstats receiver allows you to collect metrics related to the usage of CPU, memory, network, and more.
install:
  primary:
    nerdlet:
      nerdletId: marketplace.install-data-source
      nerdletState:
        dataSourceId: otel-docker
        frameworkConfigId: otel-docker
      requiresAccount: false

icon: logo.svg

keywords:
  - infrastructure
  - containers
  - open telemetry
  - docker
  - featured
categoryTerms:
  - containers
