id: 980ab07a-3483-4b44-bbac-ed9c0a9a41a1
# Sets the URL name of the quickstart on public I/O (required)
slug: Cilium-Prometheus

# Displayed in the UI (required)
title: Cilium (Prometheus)

# Long-form description of the quickstart (required)
description: |-
  ## What is Cilium?

  Cilium is an open source software for providing, securing and observing network connectivity between container workloads - cloud native, and fueled by the revolutionary Kernel technology eBPF.
  
  ## Cilium (Prometheus) quickstart highlights

  The New Relic quickstart uses dashboards to proactively monitor your Cilium servers, like:

  - Cilium Node details
  - Cilium Pod Events
  - API Call latency/IPV4 TCP
  - Open File Descriptors
  - Forwarded Packets vs Dropped Packets

  ### Requirements:
  1. [Prometheus Server](https://prometheus.io/docs/prometheus/latest/installation/) - Prometheus Remote Write
  2. Enable Prometheus [Remote Write](https://docs.newrelic.com/docs/infrastructure/prometheus-integrations/install-configure-remote-write/set-your-prometheus-remote-write-integration/) to New Relic.
  3. Scrape configuration to `prometheus.yml` config file can be found at https://docs.cilium.io/en/stable/operations/metrics/ 
  ```
  Install Cilium  by using the below helm command and also user can view hubble metrics

  helm install cilium cilium/cilium --version 1.12.2 \
  --namespace kube-system \
  --set prometheus.enabled=true \
  --set operator.prometheus.enabled=true \
  --set hubble.metrics.enabled="{dns,drop,tcp,flow,icmp,http}"

  ```

# Displayed in search results and recommendations. Summarizes a quickstarts functionality.
summary: |
  Monitor Cilium with the prometheus remote writter plugin in New Relic.

# Support level: New Relic | Verified | Community (required)
level: Community

# Authors of the quickstart (required)
authors:
  - Subhani Shaik @kinect-consulting.com

# Reference to install plans located under /install directory
# Allows us to construct reusable "install plans" and just use their ID in the quickstart config
installPlans:
  - prometheus-remote-write-integration

# Keywords for filtering / searching criteria in the UI
keywords:
  - cilium
  - prometheus
  - remote
  - write

dashboards:
  - cilium-prometheus

alertPolicies:
  - cilium-prometheus

# Documentation references
documentation:
  - name: Cilium Prometheus Metrics Plugin
    url:  https://docs.cilium.io/en/stable/operations/metrics/ 

# Content/Design
icon: logo.svg
website: https://www.newrelic.com

fallback:
  mode: link
  destination:
    url: https://docs.newrelic.com/docs/infrastructure/prometheus-integrations/get-started/send-prometheus-metric-data-new-relic/