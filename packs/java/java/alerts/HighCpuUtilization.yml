---
nrqlCondition:
  description:
  name: High CPU Utilization
  nrql:
    query:
      SELECT rate(sum(apm.service.cpu.usertime.utilization), 1 second) * 100
      as cpuUsage FROM Metric WHERE appName like '%'
  signal:
    aggregationWindow: 60
    evaluationOffset: 3
    fillOption: NONE
    fillValue:
  terms:
    - operator: ABOVE
      priority: CRITICAL
      threshold: 225
      thresholdDuration: 300
      thresholdOccurrences: ALL
  valueFunction: SINGLE_VALUE
  violationTimeLimitSeconds: 2592000
---

