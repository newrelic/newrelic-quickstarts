id: setup-openai
name: OpenAI integration
title: OpenAI integration
description: A lightweight tool to monitor your OpenAI workload.

target:
  type: integration
  destination: cloud

install:
  mode: nerdlet
  destination:
    nerdletId: marketplace.install-data-source
    nerdletState:
      dataSourceId: llm-application
