id: setup-kubecost-agent
name: Kubecost Integration
title: Kubecost
description: Use the Kubecost integration with New Relic to identify the costs associated with each of your Kubernetes resources.

target:
  type: integration
  destination: kubernetes

install:
  mode: link
  destination:
    url: https://docs.newrelic.com/docs/kubernetes-pixie/kubecost/get-started-kubecost/
