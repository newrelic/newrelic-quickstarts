id: setup-java-targeted-install
name: Java agent
title: Java agent installation
description: Start monitoring the performance of a Java application or service by installing the New Relic Java agent.

target:
  type: agent
  destination: application

install:
  mode: targetedInstall
  destination:
    recipeName: java-agent-installer

fallback:
  mode: nerdlet
  destination:
    nerdletId: setup-nerdlets.setup-java-integration
